export async function inpaint(
    image: Buffer,
    mask: Uint8Array,
    width: number,
    height: number
) {

    /*
        Future:
        OpenCV inpainting

        cv.inpaint(
            image,
            mask,
            result,
            3,
            cv.INPAINT_TELEA
        )

    */


    return image;
}